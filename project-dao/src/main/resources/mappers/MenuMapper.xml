<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nf.dao.MenuDao">
    <select id="findMenuById" resultType="com.nf.entity.MenuEntity">
        select *
        from menu
        where id = #{id}
    </select>
    <select id="getAllMenus" resultType="com.nf.entity.MenuEntity">
        select *
        from menu
    </select>
</mapper>