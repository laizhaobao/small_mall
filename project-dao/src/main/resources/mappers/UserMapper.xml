<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nf.dao.UserDao">
    <resultMap id="userResultMap" type="com.nf.entity.UserEntity">
        <id property="uid" column="uid"></id>
        <result property="username" column="username"></result>
        <result property="createTime" column="create_time"></result>
        <result property="updateTime" column="update_time"></result>
    </resultMap>
    <select id="getPageHelper" resultMap="userResultMap">
        select uid, username,pwd,email,phone,question,answer,role,create_time,update_time
        from small_user
        order by uid
    </select>
    <!--    登陆查询-->
    <select id="getByName" resultMap="userResultMap">
        select uid, username,pwd,email,phone,question,answer,role,create_time,update_time
        from small_user where username = #{username}
    </select>

    <!--    条件查询-->
    <select id="searchName" resultMap="userResultMap">
        select uid, username,pwd,email,phone,question,answer,role,create_time,update_time
        from small_user
        <if test="name != null">
            where username like #{name} '%'
        </if>
        order by id
    </select>


    <insert id="userInsert">
        INSERT INTO small_user(username,pwd,email,phone,question,answer,role,create_time,update_time)
        VALUES (#{username},#{pwd},#{email},#{phone}, #{question}, #{answer},1,now(), now());
    </insert>
    <delete id="userDelete">
        delete from small_user where uid = #{id}
    </delete>
    <update id="userUpdate">
        update small_user set username=#{username},pwd=#{pwd},email=#{email},phone=#{phone},question=#{question},
        answer=#{answer},update_time=now() where uid = #{uid}
    </update>
</mapper>